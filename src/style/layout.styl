@require 'variables'
[outline]
  [outline] *
    outline: 0.25vmin solid blue

body
  display: flex
  align-items: center
  flex-direction: column
  margin: base-line
  header
    width: 100%
    max-width: page-width
    & > nav
      display: flex
      & > a:first-child
        margin-right: base-line
      & > a > h1
        line-height: 1
